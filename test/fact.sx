function fact (arg)
	if !arg then 1 else arg * fact (arg - 1) end
end

max = 0 + argv[1] # convert to number

num = 0
while fact (num + 1) <= max do
	num ++
end 

printl ('Smallest factorial within ', max, ': fact(', num, ') = ', fact (num))
