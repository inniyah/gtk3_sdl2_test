public
main () {
	a = ['abc','def','ghi'];
	b = [1, 2, 3];

	printl(a);
	printl(b);

	str = '';
	foreach(i in a)
		str = str + i;
	printl(str);

	num = 0;
	foreach(i in b)
		num += i;
	printl(num);

	if (str != 'abcdefghi' || num != 6) {
		printl ('Failure');
		return;
	}

	str = '';
	iter = a.iter();
	while (i = iter.next())
		str = str + i;
	printl (str);

	num = 0;
	iter = b.iter();
	while (i = iter.next())
		num += i;
	printl(num);

	if (str != 'abcdefghi' || num != 6) {
		printl ('Failure');
		return;
	}

	printl ('Success');
} 
