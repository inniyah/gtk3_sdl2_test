main () {
	a = 10;

	a += 10;
	a -= 5;

	c = a --;
	b = --a;

	printl ('a: ', a);
	printl ('b: ', b);
	printl ('c: ', c);

	if (a != 13 || c != 15 || b != 13) {
		printl ('in here');
		return 1;
	}

	return 0;
}
