test () {
	for i in a do {
		for (n = 3; n <= 9; n += 3) {
			printl ('In - ', n, ':', i);
		}
	}
}

scope () {
	local a = [1,2,3];
	test ();
}

main () {
	global a = ['abc','def','ghi'];

	scope();
}
