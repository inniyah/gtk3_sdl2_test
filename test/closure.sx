snowball (count, call)
{
	value = 0;
	for (i = 0; i < count; ++i)
		value += (call)(value);
	return value;
}

public
main()
{
	inc = 2;

	total = snowball(10) do (value ; inc) {
			return value + inc;
		};

	printl ('Total: ', total);
}
