f (a) {
	local b = 2;
	global c = a;
	return global a;
}

main () {
	global a = 1;

	b = 3;
	d = f (10);

	if (a != 1 || b != 3 || c != 10 || d != 1)
		print ('Invalid: ', a, ',', b, ',', c, ',', d);
}
