attempt (a,b) {
	# cheat - do add to make num
	return 0 + a > 0 + b;
}
fail () {
	printl ('Failure');
	return 1;
}
succeed () {
	printl ('Success');
	return 1;
}

main (argv) {
	if ((attempt (argv[1], argv[2]) && succeed())) return 0;
	if ((attempt (argv[1], argv[2]) || fail())) return 1;
}
